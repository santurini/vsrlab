defaults:
  - data: default
  - logger: default
  - model: baby_vrt_moe

metric:
  _target_: core.metrics.MetricCollection
  metrics:
    PSNR:
      _target_: piqa.PSNR
    SSIM:
      _target_: piqa.SSIM

seed_index: null

max_epochs: 5000000
restore: null