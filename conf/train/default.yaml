# reproducibility
seed_index: null
deterministic: False

restore: null

ddp: False

monitor:
  metric: 'loss/val'
  mode: 'min'

defaults:
  - trainer: default
  - callbacks: default
  - logger: default
  - strategy: default

