# @package _global_
defaults:
  - override /train/model: moevrt

train:
  ddp: true

  model:
    num_experts: 4
    num_gpus: 1
    top_k: 2
    # depths: [ 4, 4, 4, 4, 4, 8, 8 ]
    # embed_dims: [ 32, 32, 32, 32, 32, 64, 64 ]
    # um_heads: [ 4, 4, 4, 4, 4, 8, 8 ]

  optimizer:
    lr: 1e-4
    betas: [ 0.9, 0.99 ]

  scheduler:
    T_max: 9e5
    eta_min: 1e-6

  data:
    batch_size: 4
    datasets:
      train:
        seq: 6

  num_grad_acc: 4
  # restore: '/home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/segmana/checkpoint.tar'
  # restore_opt: true