# @package _global_
core:
  run_id: 'realbasic_curriculum_mid'
  run_name: "Real Basic Curriculum - Mid Schedule"

defaults:
  - override /train/strategy: ddp
  - override /nn/data: mid_schedule

finetune: /home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/realbasic_curriculum_easy/checkpoints/last.ckpt

nn:
  module:
    optimizer:
      lr: 6.7e-5
    scheduler:
      eta_min: 3.4e-5
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 21000
    gradient_clip_val: 1
