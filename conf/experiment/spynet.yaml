# @package _global_
defaults:
  - override /train: distillation

train:
  ddp: True
  restore: "/Desktop/nn-lab/storage/video-super-resolution/kjhsdvq/checkpoint/4.ckpt"
  finetune: False

  data:
    batch_size: 32
    num_workers: 12
    prefetch_factor: 8

  cleaner:
    _target_: core.modules.conv.IterativeRefinement
    mid_ch: 64
    blocks: 3
    steps: 5

  cleaner_ckpt: "/home/aghinassi/Desktop/checkpoints/cleaner.ckpt"

  optimizer:
    lr: 1e-4
    betas: [ 0.9, 0.999 ]

  scheduler:
    eta_min: 1e-5

  max_epochs: 5
  k: 5
