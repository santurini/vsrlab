# @package _global_
defaults:
  - override /train: distillation

train:
  ddp: False

  teacher:
    name: "gmflow"
    ckpt: "kitti"
    input_size: [ 382, 512 ]

  cleaner:
    _target_: core.modules.conv.IterativeRefinement
    mid_ch: 64
    blocks: 3
    steps: 5

  cleaner_ckpt: "/home/aghinassi/Desktop/checkpoints/cleaner.ckpt"

  optimizer:
    lr: 1e-4
    betas: [ 0.9, 0.999 ]

  scheduler:
    eta_min: 1e-5

  max_epochs: 50
  k: 5
