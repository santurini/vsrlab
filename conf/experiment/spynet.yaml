# @package _global_
defaults:
  - override /train: distillation

train:
  ddp: True

  data:
    batch_size: 16
    num_workers: 8
    prefetch_factor: 4

  cleaner:
    _target_: core.modules.conv.IterativeRefinement
    mid_ch: 64
    blocks: 3
    steps: 5

  cleaner_ckpt: "/home/aghinassi/Desktop/checkpoints/cleaner.ckpt"

  optimizer:
    lr: 1e-4
    betas: [ 0.9, 0.99 ]

  scheduler:
    T_max: 1e9
    eta_min: 1e-4

  max_epochs: [ 250, 100, 100, 100, 50 ]
  k: 5
  start_k: 0

  restore: "/home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/oowbreta/checkpoint_0.tar"
  finetune: False