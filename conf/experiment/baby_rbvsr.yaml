# @package _global_
core:
  run_id: 'baby_vsr_default2'
  run_name: "Baby Real Basic - Pretrain 2"

defaults:
  - override /nn/module/scheduler: cosine
  - override /train/strategy: ddp
  - override /nn/data: easy_schedule

finetune: False

nn:
  module:
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 100000
    max_epochs: 1000
    gradient_clip_val: 1
