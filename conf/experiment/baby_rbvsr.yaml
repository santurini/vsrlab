# @package _global_
core:
  run_id: 'baby_vsr_hardd'
  run_name: "Baby Real Basic - Hard Schedule"

defaults:
  - override /nn/module/scheduler: cosine
  - override /train/strategy: ddp
  - override /nn/data: hard_schedule

finetune: /home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/baby_vsr_mid/checkpoints/last.ckpt

nn:
  module:
    optimizer:
      lr: 1e-6
    scheduler:
      eta_min: 6e-7
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 100000
    max_epochs: 1000
    gradient_clip_val: 1
