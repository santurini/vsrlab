# @package _global_
core:
  run_id: 'baby_vsr_hard'
  run_name: "Baby Real Basic - Hard Schedule"

defaults:
  - override /nn/module/scheduler: cosine_warmup
  - override /train/strategy: ddp
  - override /nn/data: hard_schedule

finetune: /home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/baby_vsr_mid/checkpoints/last.ckpt

nn:
  module:
    optimizer:
      lr: 1e-5
    scheduler:
      min_lrs_pow: 1
      first_cycle_steps: 21000
      warmup_steps: 2000
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 100000
    max_epochs: 1000
    gradient_clip_val: 1
