# @package _global_
core:
  run_id: 'qwdqeffgwe'
  run_name: "Real Basic Default - GAN (Adv 2e-5, Per 1e-1)"

finetune: '/home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/realbasicvsr_standard/checkpoints/last.ckpt'
from_lightning: True

defaults:
  - override /nn/data: default
  - override /nn/module: gan
  - override /nn/module/optimizer: gan_adam8bit

nn:
  data:
    batch_size: 8
    prefetch_factor: 4
    num_workers: 8
    datasets:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"
        hr_augmentation:
          - _target_: kornia.augmentation.RandomCrop
            size: [ 256, 256 ]
            pad_if_needed: True
            same_on_batch: True
            keepdim: True

          - _target_: kornia.augmentation.RandomHorizontalFlip
            p: 0.5
            same_on_batch: True
            keepdim: True

          - _target_: kornia.augmentation.RandomVerticalFlip
            p: 0.5
            same_on_batch: True
            keepdim: True

train:
  ddp: True
  trainer:
    devices: 1
    max_epochs: 1000000
    gradient_clip_val: 1
    num_grad_acc: 4




