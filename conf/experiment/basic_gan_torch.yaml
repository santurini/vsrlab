# @package _global_
core:
  run_id: 'vqfrbmnbid'
  run_name: "Real Basic Hard - GAN (Adv 2e-5, Per 1e-1)"

finetune: '/home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/realbasicvsr_curriculum_hard/checkpoints/last.ckpt'

defaults:
  - override /nn/data: default
  - override /nn/module: gan

nn:
  data:
    batch_size: 8
    prefetch_factor: 4
    num_workers: 8
    datasets:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"
        hr_augmentation:
          - _target_: kornia.augmentation.RandomCrop
            size: [ 256, 256 ]
            pad_if_needed: True
            same_on_batch: True
            keepdim: True

          - _target_: kornia.augmentation.RandomHorizontalFlip
            p: 0.5
            same_on_batch: True
            keepdim: True

          - _target_: kornia.augmentation.RandomVerticalFlip
            p: 0.5
            same_on_batch: True
            keepdim: True

train:
  ddp: False
  trainer:
    devices: 1
    max_epochs: 1000000
    gradient_clip_val: 1
    num_grad_acc: 4




