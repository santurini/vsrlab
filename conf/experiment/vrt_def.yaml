# @package _global_
defaults:
  - override /train/model: baby_vrt

core:
  run_name: "Real Basic Default"

train:
  optimizer:
    lr: 1e-4
    betas: [ 0.9, 0.99 ]

  scheduler:
    eta_min: 1e-7

  data:
    batch_size: 2
    datasets:
      train:
        seq: 6
