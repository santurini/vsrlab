# @package _global_
core:
  run_id: 'realbasic_curriculum_hard'
  run_name: "Real Basic Curriculum - Mid Schedule"

defaults:
  - override /train/strategy: ddp
  - override /nn/data: hard_schedule

finetune: /home/aghinassi/Desktop/nn-lab/storage/video-super-resolution/realbasic_curriculum_mid/checkpoints/last.ckpt

nn:
  module:
    optimizer:
      lr: 3.4e-5
    scheduler:
      eta_min: 1e-6
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 21000
    gradient_clip_val: 1
