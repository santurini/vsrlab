# @package _global_
cfg_dir: /home/aghinassi/VSR/checkpoints/vrt_gan_of
lr_dir: /dev/shm/compressed
hr_dir: /dev/shm/groundtruth
out_dir: /mnt/hdd/dataset/pexels

window_size: 24
num_workers: 8

metric:
  _target_: core.metrics.MetricCollection
  metrics:
    PSNR:
      _target_: piqa.PSNR
    SSIM:
      _target_: piqa.SSIM