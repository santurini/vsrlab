# @package _global_
core:
  run_id: 'baby_vsr_{pretrain}_gan'
  run_name: "Real Basic {Pretrain} - GAN"

defaults:
  - override /nn/data: default
  - override /nn/module: gan

nn:
  data:
    dataset:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"

finetune: '/home/aghinassi/VSR/{pretrain}.ckpt'