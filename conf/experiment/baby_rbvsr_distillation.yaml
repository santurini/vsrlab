# @package _global_
core:
  run_id: 'baby_vsr_distilled'
  run_name: "Baby Real Basic - Distilled Pretrain"

defaults:
  - override /nn/module: distillation
  - override /nn/module/optimizer: deepspeed
  - override /train/trainer: deepspeed
  - override /train/strategy: deepspeed