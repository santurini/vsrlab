# @package _global_
core:
  run_id: 'realbasic_curriculum_easy'
  run_name: "Real Basic Curriculum - Easy Schedule"

defaults:
  - override /train/strategy: ddp
  - override /nn/data: easy_schedule

finetune: null

nn:
  module:
    optimizer:
      lr: 1e-4
    scheduler:
      eta_min: 6.7e-5
    set_lr: False

train:
  trainer:
    devices: 2
    max_steps: 21000
    gradient_clip_val: 1
