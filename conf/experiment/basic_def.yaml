# @package _global_
core:
  run_id: 'realbasicvsr_standard'
  run_name: "Real Basic Default"

defaults:
  - override /train/strategy: ddp

finetune: null

nn:
  data:
    datasets:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"

train:
  trainer:
    devices: 2
    max_steps: 60000
    gradient_clip_val: 1
