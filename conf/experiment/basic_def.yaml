# @package _global_
core:
  run_id: 'realbasic_default'
  run_name: "Real Basic Default"

defaults:
  - override /train/strategy: ddp

finetune: null

nn:
  data:
    dataset:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"

train:
  trainer:
    devices: 2
    max_steps: 63000
    gradient_clip_val: 1
