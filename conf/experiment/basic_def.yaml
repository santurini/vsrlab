# @package _global_
core:
  run_name: "Real Basic Default - IRR"

defaults:
  - override /train/strategy: ddp

finetune: null

nn:
  module:
    model:
      optical_flow: "raft"
  data:
    datasets:
      train:
        path: "/home/aghinassi/Desktop/MergedVSR"

train:
  trainer:
    devices: 2
    max_steps: 60000
    gradient_clip_val: 1
