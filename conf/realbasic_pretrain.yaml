core:
  project_name: 'video-super-resolution'
  storage_dir: ${oc.env:PROJECT_ROOT}/core
  version: 0.0.1
  tags: null

defaults:
  - hydra: default.yaml
  - nn: realbasic_pretrain.yaml
  - train: realbasic_pretrain.yaml
  - _self_