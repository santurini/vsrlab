_target_: vsr.models.RealBasicVSR.pl_module.LitBase
log_interval: 200

set_lr: False

metric:
  _target_: core.metrics.MetricCollection
  metrics:
    PSNR:
      _target_: piqa.PSNR
    SSIM:
      _target_: piqa.SSIM

defaults:
  - model: baby_realbasic
  - optimizer: adam
  - scheduler: cosine