core:
  project_name: 'video-super-resolution'
  run_id: 'baby_rbvsr_gan'
  run_name: "Baby Real Basic - Finetune"
  storage_dir: ${oc.env:LOGGING_DIR}
  version: 0.0.1
  tags: null

cfg.nn.data.batch_size: 2

defaults:
  - hydra: default
  - nn: default
  - train: default
  - override nn/module: gan
  - _self_