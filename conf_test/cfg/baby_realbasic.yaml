defaults:
  - hydra: default
  - nn: default
  - train: default
  - override nn/module/model: baby_realbasic
  - override nn/module/optimizer: ${master.core.strategy}
  - override nn/module/scheduler: cosine_warmup
  - override train/trainer: ${master.core.strategy}
  - override train/strategy: ${master.core.strategy}
  - override nn/data: reds_diag
  - _self_