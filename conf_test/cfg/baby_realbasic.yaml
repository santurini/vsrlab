defaults:
  - hydra: default
  - nn: default
  - train: default
  - override nn/module/model: baby_realbasic
  - override nn/module/optimizer: ${cfg.core.strategy}
  - override nn/module/scheduler: cosine_warmup
  - override train/trainer: ${cfg.core.strategy}
  - override train/strategy: ${cfg.core.strategy}
  - override nn/data: reds_diag
  - _self_